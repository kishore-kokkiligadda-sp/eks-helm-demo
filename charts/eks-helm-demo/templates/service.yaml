apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-helm-demo.fullname" . }}
  labels:
    {{- include "eks-helm-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-helm-demo.selectorLabels" . | nindent 4 }}
