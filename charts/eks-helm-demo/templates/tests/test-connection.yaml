apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-helm-demo.fullname" . }}-test-connection"
  labels:
{{ include "eks-helm-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eks-helm-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
